Original `.ipynb` source files live here, tracked in the repo. Run
`scripts/convert-notebooks.sh` locally to (re)generate the static HTML
in `assets/notebooks/` and a page stub in `notebooks/` for any new or
changed notebook.
